apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-nginx
  labels:
    app: {{ .Release.Name }}-nginx
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-web
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.service.nodePort }}
